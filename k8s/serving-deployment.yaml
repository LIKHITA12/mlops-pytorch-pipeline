apiVersion: apps/v1
kind: Deployment
metadata:
  name: pytorch-serving
  namespace: mlops-pytorch
spec:
  replicas: 2
  selector:
    matchLabels:
      app: pytorch-serving
  template:
    metadata:
      labels:
        app: pytorch-serving
    spec:
      containers:
        - name: serve
          image: mlops-pytorch-serve:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
